Scaffold a new component, module, or feature following the existing patterns in this codebase.

Steps:
1. Explore the codebase to identify naming conventions, file structure, and patterns
2. Create the minimum set of files needed (do not over-engineer)
3. Follow existing conventions exactly — do not introduce new patterns
4. Wire it up to any existing entry points (index files, registries, etc.)

$ARGUMENTS
